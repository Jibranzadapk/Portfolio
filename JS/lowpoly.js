







































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































 document.addEventListener("DOMContentLoaded", function () { const canvas = document.createElement('canvas'); document.body.appendChild(canvas); const ctx = canvas.getContext('2d'); let width = window.innerWidth; let height = window.innerHeight; canvas.width = width; canvas.height = height; const dots = []; const numDots = 80; const getRandomColor = () => { return 'rgba(0, 255, 0, 0.4)'; }; class Dot { constructor() { this.x = Math.random() * width; this.y = Math.random() * height; this.radius = 4; this.velocity = { x: (Math.random() - 0.5) * 2, y: (Math.random() - 0.5) * 2 }; } move() { this.x += this.velocity.x; this.y += this.velocity.y; if (this.x <= 0 || this.x >= width) this.velocity.x *= -1; if (this.y <= 0 || this.y >= height) this.velocity.y *= -1; } draw() { ctx.beginPath(); ctx.arc(this.x, this.y, this.radius, 0, Math.PI * 2); ctx.fillStyle = '#00FF00'; ctx.fill(); } } for (let i = 0; i < numDots; i++) { dots.push(new Dot()); } const connectDots = () => { for (let i = 0; i < dots.length; i++) { for (let j = i + 1; j < dots.length; j++) { let dist = Math.sqrt(Math.pow(dots[i].x - dots[j].x, 2) + Math.pow(dots[i].y - dots[j].y, 2)); if (dist < 150) { ctx.beginPath(); ctx.moveTo(dots[i].x, dots[i].y); ctx.lineTo(dots[j].x, dots[j].y); ctx.strokeStyle = getRandomColor(); ctx.lineWidth = 0.5; ctx.stroke(); } } } }; const animate = () => { ctx.clearRect(0, 0, width, height); for (let i = 0; i < dots.length; i++) { dots[i].move(); dots[i].draw(); } connectDots(); requestAnimationFrame(animate); }; animate(); window.addEventListener('resize', () => { width = window.innerWidth; height = window.innerHeight; canvas.width = width; canvas.height = height; }); });